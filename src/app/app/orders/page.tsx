import { FeaturePlaceholder } from "@/components/features/app/feature-placeholder";

export default function OrdersPage() {
  return (
    <FeaturePlaceholder
      title="Đơn Hàng"
      description="Lịch sử đơn hàng, filter, pagination và trạng thái conversion sẽ được triển khai ở sprint sau."
    />
  );
}
